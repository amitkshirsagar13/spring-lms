info:
  component: Hystrix Turbine Dashboard
server:
  hostname: localhost.shinigami.io
  port: 1234
security:
  basic:
    enabled: true
  user:
    name: hystrix
    password: password
applications: ZUUL
turbine:
  clusterNameExpression: new String("default")
  appConfig: ${applications}