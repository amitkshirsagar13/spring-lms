info:
  component: Hystrix Turbine Dashboard
server:
  hostname: localhost.shinigami.io
  port: 1234
security:
  basic:
    enabled: true
  user:
    name: hystrix
    password: password
applications: COURSE-CATAGORY
turbine:
  aggregator:
    clusterConfig: ${applications}
  appConfig: ${applications}